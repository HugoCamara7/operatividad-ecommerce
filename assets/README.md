# Assets

Coloque aquí `forus_logo.png` para que aparezca en la pantalla de acceso.

Si el archivo no existe, el login usa la marca tipográfica **OC** como
alternativa: no es obligatorio para que la aplicación funcione.

Tamaño recomendado: PNG con fondo transparente, ~200×60 px.
